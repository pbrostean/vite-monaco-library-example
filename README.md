# Vite Monaco Library Example

## Getting Started

1. Install dependencies:

   ```sh
   pnpm install
   ```

2. Build the project:

   ```sh
   pnpm run build
   ```

3. Run the project:

    ```sh
    pnpm run dev
    ```
